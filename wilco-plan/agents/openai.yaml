interface:
  display_name: "Wilco Plan"
  short_description: "Default to plan-only and write Wilco execution plans"
  default_prompt: "Default to plan-only, reuse an existing active slug when appropriate, and update the active execution plan under .wilco/plans/active with derived index sync. If a new tracked slug does not exist yet, route to wilco-init instead of bootstrapping it here."

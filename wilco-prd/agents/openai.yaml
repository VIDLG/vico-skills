interface:
  display_name: "Wilco PRD"
  short_description: "Create PRDs only when plan-only is not enough"
  default_prompt: "Treat PRD creation as a hard escalation gate, not a default. Explore the codebase, decide whether scope truly justifies a PRD beyond the plan, route back to wilco-plan when it does not, and use wilco-init first if a new tracked slug has not been bootstrapped yet."

interface:
  display_name: "Vico Ground"
  short_description: "Build shared ground before planning or execution"
  default_prompt: "Build actionable shared ground for the current target. Choose the highest-value grounding move, keep findings and issues distinct, surface assumptions and tradeoffs explicitly, ask rather than guess when uncertainty is material, and emit a self-contained ground handoff when the next consumer is vico-plan."
